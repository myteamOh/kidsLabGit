<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kidslab.client.mypage.dao.MypageDao">

	<select id="parentInfo" parameterType="int" resultType="parent">
		SELECT
		parent_no, parent_id, parent_name,
		parent_phone, parent_smsagree,
		parent_emailagree, parent_kakaoagree,
		parent_knowroute, parent_address,
		parent_termsagree, parent_registerdate, parent_status
		FROM parent
		WHERE
		parent_no = #{parent_no}
	</select>

</mapper>
