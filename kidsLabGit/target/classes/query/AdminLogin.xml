<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kidslab.admin.login.dao.LoginDao">

	<select id="loginCheck" resultType="java.lang.String"
		parameterType="login">
		SELECT admin_id
		FROM admin
		WHERE admin_id = #{userId} AND
		admin_password
		= #{userPw}
	</select>

	<select id="viewMember" resultType="login">
		SELECT
		admin_id,
		admin_password
		FROM admin
		where
		admin_id = #{userId} and
		admin_password =
		#{userPw}
	</select>

</mapper>
